# Sparkle disabled unit-test backlog

Last audited: **2026-09-07** against `docs/unit_test_plan.md` and all sources compiled by `SparkleTestSuite`.

Current inventory: **0 disabled behaviors**, matching the absence of `DISABLED_` test instances in the test sources.

This file is the canonical disabled-test inventory. Add an entry whenever a behavior is represented by a disabled test, and remove it when the behavior is either covered by an enabled test or deliberately removed as an invalid/private implementation-level requirement.
