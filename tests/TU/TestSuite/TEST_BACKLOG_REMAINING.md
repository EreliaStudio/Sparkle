# Sparkle missing and partial unit-test backlog

Last audited: **2026-09-05** against the current public API and `SparkleTestSuite` sources.

No missing test implementations remain from the 110-entry inventory.

Completed and already-covered entries have been removed. As in the original
inventory, an implemented test is removed regardless of its enabled/disabled
status. This does **not** mean every specified behavior is implemented by the
library or that the entire test suite passes.

See [the coverage audit](TEST_BACKLOG_COVERAGE.md) for the test-file mapping,
current API interpretations, implemented disabled regressions, and validation
results. The separate [disabled-specification backlog](TEST_BACKLOG_DISABLED.md)
was outside this audit's scope.
