# Disabled-test decision catalogue

Last audited: **2026-09-07**.

The source tree contains **0 disabled tests**. Previously catalogued public behaviors are covered by enabled tests. The duplicate application-runtime registration case and synthetic OpenGL object-creation failures were removed because they specified private implementation or required test-only indirection in production code.

The canonical inventory is `TEST_BACKLOG_DISABLED.md`.
