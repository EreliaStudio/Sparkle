@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

find_dependency(GLEW REQUIRED)
find_dependency(OpenGL REQUIRED)

set_and_check(Sparkle_INCLUDE_DIR "@PACKAGE_INCLUDE_INSTALL_DIR@")
set(Sparkle_INCLUDE_DIRS "${Sparkle_INCLUDE_DIR}")
set(Sparkle_DIR "${CMAKE_CURRENT_LIST_DIR}")

include("${CMAKE_CURRENT_LIST_DIR}/SparkleTargets.cmake")
check_required_components(Sparkle)
